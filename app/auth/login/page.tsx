import LoginForm from "./LoginForm";

export const metadata = {
  title: "Sign In — Spendly",
  description: "Sign in to your Spendly account",
};

export default function LoginPage() {
  return <LoginForm />;
}