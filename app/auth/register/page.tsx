import RegisterForm from "./RegisterForm";

export const metadata = {
  title: "Create Account — Spendly",
  description: "Create a new Spendly account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}